@page
@model RegisterModel
@{
    ViewData["Title"] = "Реєстрація";
}

<div class="bs-auth-shell">
    <section class="bs-auth-panel" aria-labelledby="register-title">
        <div class="bs-auth-head">
            <a asp-page="/Index" class="bs-auth-brand" aria-label="BookSlot">
                <span class="bs-auth-mark" aria-hidden="true"><span></span></span>
                <span>BookSlot</span>
            </a>
            <div class="bs-auth-kicker">// старт простору</div>
            <h1 id="register-title">Створити акаунт</h1>
            <p>Запустіть сторінку запису, розклад і чистий дашборд для свого бізнесу.</p>
        </div>

        <div class="bs-auth-card">
            <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                <div class="mb-3">
                    <label asp-for="Input.Email" class="form-label fw-semibold">Email</label>
                    <input asp-for="Input.Email" class="form-control" placeholder="your@email.com" autocomplete="email" />
                    <span asp-validation-for="Input.Email" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Input.Password" class="form-label fw-semibold">Пароль</label>
                    <input asp-for="Input.Password" class="form-control" placeholder="мінімум 6 символів, одна цифра" autocomplete="new-password" />
                    <span asp-validation-for="Input.Password" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Input.ConfirmPassword" class="form-label fw-semibold">Підтвердити пароль</label>
                    <input asp-for="Input.ConfirmPassword" class="form-control" placeholder="повторіть пароль" autocomplete="new-password" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
                </div>

                <button type="submit" class="btn btn-primary bs-auth-submit w-100">
                    Зареєструватись
                </button>
            </form>
        </div>

        <p class="bs-auth-switch">
            Вже є акаунт? <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Увійти</a>
        </p>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
